#pragma once

#include <string>

namespace pyramid {
namespace core {

  /// \brief Placeholder function in the core library
  /// \return A greeting string
  std::string getCoreGreeting();

  // Add declarations for common functions, classes (Logger, ThreadPool, etc.)
  // Add declarations for autonomy primitives (GOAP, FSM, BTs)

} // namespace core
} // namespace pyramid 



project Test_Pcl_Plugin_Loader is
  Root_Dir := external("UNMANNED_ROOT", ".." & "/.." & "/..");
  Pcl_Include_Dir := external("PCL_INCLUDE_DIR", Root_Dir & "/include");
  Pcl_Lib_Dir := external("PCL_LIB_DIR", Root_Dir & "/build/install/lib");
  Pcl_Lib_Name := external("PCL_LIB_NAME", "pcl_core");

  for Languages use ("Ada");
  for Source_Dirs use
    (".",
     Root_Dir & "/subprojects/PCL/bindings/ada");
  for Main use ("test_pcl_plugin_loader.adb");

  for Object_Dir use "obj_plugin_loader";
  for Exec_Dir use "bin";

  package Compiler is
    for Default_Switches ("Ada") use ("-g");
  end Compiler;

  type OS_Type is ("Windows_NT", "linux", "darwin");
  OS : OS_Type := external ("OS", "linux");

  package Linker is
    case OS is
      when "Windows_NT" =>
        for Default_Switches ("Ada") use
          ("-L" & Pcl_Lib_Dir,
           "-l" & Pcl_Lib_Name,
           "-lws2_32",
           "-lkernel32");
      when others =>
        for Default_Switches ("Ada") use
          ("-L" & Pcl_Lib_Dir,
           "-l" & Pcl_Lib_Name,
           "-ldl");
    end case;
  end Linker;
end Test_Pcl_Plugin_Loader;
